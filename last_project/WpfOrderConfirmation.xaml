<UserControl x:Class="last_project.WpfOrderConfirmation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#FF1E1E1E" Foreground="White">

    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#FF3A3A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="발주 확인 (Order Confirmation)" FontSize="24" FontWeight="Bold" Foreground="White"/>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="OrderGrid" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  Background="#FF2D2D30"
                  RowBackground="#FF2D2D30"
                  AlternatingRowBackground="#FF333333"
                  Foreground="White"
                  BorderBrush="#FF444444"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  ColumnHeaderHeight="40"
                  RowHeight="40"
                  SelectionMode="Single"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#FF111111"/>
                    <Setter Property="Foreground" Value="#FFDDDDDD"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10,0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="5" Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#FF3B82F6"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="No." Binding="{Binding Id}" Width="50"/>

                <DataGridTextColumn Header="진행 상태" Binding="{Binding Status}" Width="90" FontWeight="Bold">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="대기중">
                                    <Setter Property="Foreground" Value="#FFFFA500"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="승인됨">
                                    <Setter Property="Foreground" Value="#FF4ADE80"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="취소">
                                    <Setter Property="Foreground" Value="#FFFF5555"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="발주처" Binding="{Binding Company}" Width="120" FontWeight="Bold"/>

                <DataGridTextColumn Header="품목" Binding="{Binding ItemName}" Width="*"/>

                <DataGridTextColumn Header="수량" Binding="{Binding Quantity}" Width="70"/>

                <DataGridTextColumn Header="단가" Binding="{Binding Price, StringFormat={}{0:N0}원}" Width="100"/>

                <DataGridTextColumn Header="총액" Binding="{Binding TotalPrice, StringFormat={}{0:N0}원}" Width="110" FontWeight="Bold"/>

                <DataGridTextColumn Header="발주 일자" Binding="{Binding OrderDate}" Width="180"/>

                <DataGridTextColumn Header="연락처" Binding="{Binding Contact}" Width="130"/>

            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="BtnRefresh" Content="새로고침" Click="BtnRefresh_Click"/>
            <Border Width="20"/>
            <Button x:Name="BtnApprove" Content="주문 승인" Background="#FF2563EB" Click="BtnApprove_Click"/>
            <Button x:Name="BtnCancelOrder" Content="주문 취소" Background="#FFB91C1C" Click="BtnCancelOrder_Click"/>
            <Button x:Name="BtnDelete" Content="내역 삭제" Background="#FFB91C1C" Margin="5,0,0,0" Click="BtnDelete_Click"/>
        </StackPanel>
    </Grid>
</UserControl>