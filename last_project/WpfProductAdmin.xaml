<UserControl x:Class="last_project.WpfProductAdmin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400"
             Background="#FF1E1E1E">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="26" />
            <Setter Property="Margin" Value="5, 5" />
            <Setter Property="Background" Value="#FF3A3A3A" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" Margin="5,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="FontSize" Value="13" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="30" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Background" Value="#FF4A4A4A" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF6A6A6A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="품목번호 :" />
        <TextBox x:Name="TxtItemCode" Grid.Row="0" Grid.Column="1" Width="150" HorizontalAlignment="Left" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="브랜드 :" />
        <TextBox x:Name="TxtBrand" Grid.Row="1" Grid.Column="1" Width="150" HorizontalAlignment="Left" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="색상 :" />
        <TextBox x:Name="TxtColor" Grid.Row="2" Grid.Column="1" Width="150" HorizontalAlignment="Left" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="사이즈 :" />
        <TextBox x:Name="TxtSize" Grid.Row="3" Grid.Column="1" Width="150" HorizontalAlignment="Left" />

        <TextBlock Grid.Row="4" Grid.Column="0" Text="카테고리 :" />
        <TextBox x:Name="TxtCategory" Grid.Row="4" Grid.Column="1" Width="150" HorizontalAlignment="Left" />

        <TextBlock Grid.Row="5" Grid.Column="0" Text="재고 :" />
        <TextBox x:Name="TxtStock" Grid.Row="5" Grid.Column="1" Width="150" HorizontalAlignment="Left" />

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,15,0,0">
            <Button x:Name="BtnRegister" Content="신규등록" Width="70" Click="BtnRegister_Click" />
            <Button x:Name="BtnUpdate" Content="수정" Width="60" Click="BtnUpdate_Click" />
            <Button x:Name="BtnDelete" Content="삭제" Width="60" Click="BtnDelete_Click" />
            <Button x:Name="BtnRefresh" Content="새로고침" Width="70" Click="BtnRefresh_Click" />
        </StackPanel>

    </Grid>
</UserControl>