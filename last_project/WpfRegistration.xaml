<UserControl x:Class="last_project.WpfRegistration"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" 
             Height="650" Width="450"
             Background="#FF1E1E1E" Foreground="White">

    <UserControl.Resources>
        <Style TargetType="Control" x:Key="InputBoxStyle">
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF4F4F52"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF3B82F6"/>
                                <Setter TargetName="border" Property="Background" Value="#FF252526"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF999999"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource InputBoxStyle}"/>
        <Style TargetType="PasswordBox" BasedOn="{StaticResource InputBoxStyle}"/>

        <Style x:Key="MainButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="22">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#FF3B82F6" Offset="0.0"/>
                                    <GradientStop Color="#FF2563EB" Offset="1.0"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="10" ShadowDepth="3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF60A5FA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SubButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Background" Value="#FF3E3E42"/>
            <Setter Property="Foreground" Value="#FFDDDDDD"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF525255"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40">
            <TextBlock Text="Sign Up" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
            <TextBlock Text="상세 정보 입력" FontSize="14" Foreground="#FF6B7280" HorizontalAlignment="Center" Margin="0,5,0,20"/>

            <TextBlock Text="아이디" Foreground="#FF9CA3AF" Margin="0,0,0,5"/>
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtId" Height="35" VerticalContentAlignment="Center"/>
                <Button x:Name="BtnCheckDup" Grid.Column="1" Content="확인" Margin="5,0,0,0" 
                        Style="{StaticResource SubButtonStyle}" Click="BtnCheckDup_Click"/>
            </Grid>

            <TextBlock Text="비밀번호" Foreground="#FF9CA3AF" Margin="0,0,0,5"/>
            <PasswordBox x:Name="TxtPw" Height="35" Margin="0,0,0,15"/>

            <TextBlock Text="비밀번호 확인" Foreground="#FF9CA3AF" Margin="0,0,0,5"/>
            <PasswordBox x:Name="TxtPwConfirm" Height="35" Margin="0,0,0,15"/>

            <TextBlock Text="이름 (실명)" Foreground="#FF9CA3AF" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtName" Height="35" Margin="0,0,0,15"/>

            <TextBlock Text="닉네임 (별명)" Foreground="#FF9CA3AF" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtNickname" Height="35" Margin="0,0,0,15"/>

            <TextBlock Text="생년월일 (예: 1990-01-01)" Foreground="#FF9CA3AF" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtBirthdate" Height="35" Margin="0,0,0,15"/>

            <TextBlock Text="직급 (Role)" Foreground="#FF9CA3AF" Margin="0,0,0,5"/>
            <ComboBox x:Name="CmbRole" Height="35" Margin="0,0,0,15" SelectedIndex="0">
                <ComboBoxItem Content="STAFF (일반 사원)"/>
                <ComboBoxItem Content="MANAGER (관리자)"/>
                <ComboBoxItem Content="ADMIN (최고 관리자)"/>
            </ComboBox>

            <TextBlock Text="연락처" Foreground="#FF9CA3AF" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtPhone" Height="35" Margin="0,0,0,15"/>

            <TextBlock Text="이메일" Foreground="#FF9CA3AF" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtEmail" Height="35" Margin="0,0,0,20"/>

            <Button x:Name="BtnRegister" Content="회원가입 완료" Style="{StaticResource MainButtonStyle}" Click="BtnRegister_Click"/>
            <Button x:Name="BtnCancel" Content="취소" Click="BtnCancel_Click" Margin="0,10,0,0" Background="Transparent" Foreground="Gray">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" 
                                   HorizontalAlignment="Center" TextDecorations="Underline"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </ScrollViewer>
</UserControl>