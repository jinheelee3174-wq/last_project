<UserControl x:Class="last_project.WpfSlotEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             Width="800"
             Height="450"
             Background="#FF111111">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="12" Background="#FF1E1E1E" CornerRadius="8">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Camera:" Foreground="White" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,12,0" />
                <ComboBox x:Name="CamSelector" Width="180" SelectionChanged="CamSelector_SelectionChanged">
                    <ComboBoxItem Content="Camera 1" Tag="cam1" IsSelected="True" />
                    <ComboBoxItem Content="Camera 2" Tag="cam2" />
                    <ComboBoxItem Content="Camera 3" Tag="cam3" />
                </ComboBox>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Margin="0,12,0,0" BorderBrush="#FF3A3A3A" BorderThickness="2" CornerRadius="10" Background="#FF0A0A0A">
            <Grid>
                <wv2:WebView2 x:Name="SlotWebView" />
            </Grid>
        </Border>
    </Grid>
</UserControl>