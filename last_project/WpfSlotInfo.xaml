<UserControl x:Class="last_project.WpfSlotInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="220" d:DesignWidth="400"
             Background="#FF1E1E1E">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="24" />
            <Setter Property="Margin" Value="5,2" />
            <Setter Property="Width" Value="130" />
            <Setter Property="Background" Value="#FF3A3A3A" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" Margin="5,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="Margin" Value="5,2" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="10,5" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="28" />
            <Setter Property="Width" Value="80" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Background" Value="#FF4A4A4A" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF6A6A6A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border BorderBrush="#FF3A3A3A" BorderThickness="1" CornerRadius="8" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="슬롯 아이디 :" />
            <TextBlock Grid.Row="1" Grid.Column="0" Text="좌표 X :" />
            <TextBlock Grid.Row="2" Grid.Column="0" Text="좌표 Y :" />
            <TextBlock Grid.Row="3" Grid.Column="0" Text="너비 W :" />
            <TextBlock Grid.Row="4" Grid.Column="0" Text="높이 H :" />

            <TextBox x:Name="TxtSlotId" Grid.Row="0" Grid.Column="1" />
            <TextBox x:Name="TxtSlotX" Grid.Row="1" Grid.Column="1" />
            <TextBox x:Name="TxtSlotY" Grid.Row="2" Grid.Column="1" />
            <TextBox x:Name="TxtSlotW" Grid.Row="3" Grid.Column="1" />
            <TextBox x:Name="TxtSlotH" Grid.Row="4" Grid.Column="1" />

            <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" Margin="5,10,0,0">
                <CheckBox x:Name="ChkIsActive" Content="슬롯 활성화" VerticalAlignment="Center" Margin="0,0,10,0"/>

                <Button x:Name="BtnSave" Content="저장" Click="BtnSave_Click" Margin="0,0,5,0"/>

                <Button x:Name="BtnDelete" Content="삭제" 
            Background="#FFB91C1C" 
            Click="BtnDelete_Click" />
            </StackPanel>

        </Grid>
    </Border>
</UserControl>